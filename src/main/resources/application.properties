spring.application.name=scm-system
# 1. Database Connection Setup
spring.datasource.url=jdbc:mysql://localhost:3306/scm_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Ambarkumar@47

# 2. Hibernate Configuration (The ORM Magic)
# "update" automatically creates/updates tables based on your Java Entities
spring.jpa.hibernate.ddl-auto=update

# 3. Debugging (Optional but helpful)
# Shows the SQL queries in the console so you can see what's happening
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 4. View Configuration
spring.thymeleaf.cache=false